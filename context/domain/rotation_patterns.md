# Rotation Patterns
- Pattern tokens: `1`=Day, `2`=Night, `0`=Off (12h norm for CAS).
- Example CAS pattern: `111100` with staggered offsets per employee/team.
- Enforce: S1 (pattern compliance), with overrides for ad-hoc / partial weeks.
